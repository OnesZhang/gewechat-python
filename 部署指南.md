# GeWeChat Python 项目部署指南

本文档提供了使用Docker部署GeWeChat Python项目的详细步骤。

## 一、环境准备

### 必要环境

- Docker（v19.03.0+）
- Docker Compose（v1.29.0+）

### 安装Docker（Windows）

1. 下载并安装 [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop)
2. 完成安装后启动Docker Desktop
3. 验证安装成功：打开命令行，运行 `docker --version` 和 `docker-compose --version`

## 二、部署步骤

### 1. 项目文件准备

确保项目文件结构如下：

```
gewechat-python/
  ├── app.py              # 应用入口点
  ├── config_manager.py   # 配置管理模块
  ├── database.py         # 数据库模块
  ├── ticket_manager.py   # 工单管理模块
  ├── docker-compose.yml  # Docker Compose配置文件
  ├── Dockerfile          # Docker镜像构建文件
  ├── entrypoint.sh       # 容器启动脚本
  ├── requirements.txt    # Python依赖列表
  └── .env                # 环境变量文件
```

### 2. 确保entrypoint.sh有执行权限

在命令行执行：

```bash
chmod +x entrypoint.sh
```

### 3. 构建和启动容器

在项目根目录下执行：

```bash
docker-compose up -d
```

此命令会：
- 构建应用的Docker镜像
- 创建并启动MySQL数据库容器
- 创建并启动应用容器
- 所有容器在后台运行（-d参数）

### 4. 查看运行状态和日志

检查容器运行状态：

```bash
docker-compose ps
```

查看应用日志：

```bash
docker-compose logs -f app
```

查看数据库日志：

```bash
docker-compose logs -f db
```

## 三、配置说明

### 环境变量配置

项目使用`.env`文件和Docker Compose的environment配置来管理环境变量：

1. 数据库连接信息在docker-compose.yml中配置：
   ```yaml
   environment:
     - DB_HOST=db
     - DB_PORT=3306
     - DB_USER=gewechat
     - DB_PASSWORD=gewechat_password
     - DB_NAME=gewechat
   ```

2. 应用其他配置可在`.env`文件中设置

### 数据持久化

1. 数据库数据存储在Docker卷中：`mysql-data`
2. 应用数据存储在本地目录：`./data`

这确保了容器重启或更新后数据不会丢失。

## 四、维护操作

### 停止服务

```bash
docker-compose down
```

### 重新构建并启动服务

修改代码后需要重新构建：

```bash
docker-compose up -d --build
```

### 查看容器资源使用情况

```bash
docker stats
```

### 备份数据库

```bash
docker exec gewechat-mysql sh -c 'exec mysqldump -ugewechat -pgewechat_password gewechat' > backup.sql
```

## 五、故障排除

### 1. 应用容器无法连接到数据库

检查数据库容器是否正常运行：

```bash
docker-compose ps
```

查看数据库启动日志：

```bash
docker-compose logs db
```

### 2. 权限问题

如果遇到文件权限问题，请确保entrypoint.sh具有执行权限：

```bash
chmod +x entrypoint.sh
```

### 3. 端口冲突

如果端口被占用，修改docker-compose.yml中的端口映射：

```yaml
ports:
  - "8888:8000"  # 将外部端口改为8888
```

## 六、更新与升级

### 获取最新代码

```bash
git pull
```

### 重新构建服务

```bash
docker-compose up -d --build
```

## 附录：Docker命令参考

### 常用Docker命令

- 查看所有容器：`docker ps -a`
- 进入容器命令行：`docker exec -it gewechat-python bash`
- 查看容器日志：`docker logs -f gewechat-python`
- 重启容器：`docker restart gewechat-python` 